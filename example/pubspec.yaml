name: example
description: An example app to demonstrate the use of 'Quoter' package

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.4
  flutter_lints: ^1.0.0
  mockito: ^5.0.16

flutter:
  uses-material-design: true